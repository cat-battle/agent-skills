---
name: skill-name
description: One or two sentences, written for a model deciding whether to load this. Say what the skill does AND when to use it, including concrete trigger words the user is likely to type.
---

# Skill Name

One-line statement of what this skill makes the agent do.

## When to use

- Bullet list of situations that should trigger this skill.
- Include the negative cases too: when NOT to use it.

## Process

Numbered steps the agent follows. Keep them imperative and checkable.

1. Step one.
2. Step two.
3. Step three.

## Output

What the agent produces at the end — format, location, and who it's for.

## References

Larger material lives in `references/` and is read only when needed, so it
doesn't cost context on every load.

- `references/example.md` — read when <condition>.
